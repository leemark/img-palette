name = "img-palette-api"
main = "worker.js"
compatibility_date = "2023-08-01"

# Enable Node.js compatibility
compatibility_flags = ["nodejs_compat"]

[vars]
# These will need to be set in the Cloudflare dashboard or via CLI
# GEMINI_API_KEY = "your-api-key-here"

# Define environment bindings
[env.production]
# Production-specific environment variables can be defined here

[env.staging]
# Staging-specific environment variables can be defined here 